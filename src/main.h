#pragma once

#include <vector>
#include <string>

class  Main
{
    public:
        // for catching SDL errors and printing them to console after the program shuts down
        static std::vector<std::string> errors;
};