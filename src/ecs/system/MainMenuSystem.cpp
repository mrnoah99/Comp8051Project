#include "MainMenuSystem.h"

// if a menu was implemented, would just be a check if the event is a click on somewhere
void MainMenuSystem::update(const SDL_Event &event) {
    // store button locations in the header file and process the click event here
}